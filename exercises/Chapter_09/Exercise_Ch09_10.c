/* Exercise_Ch09_10.c --  */

/*
 *  为了让程序清单 9.8 中的 to_binary() 函数更通用，编写一个 to_base_n() 函数接受两个参数，
 *  且第二个参数在 2～10 范围内，然后以第 2 个参数中指定的进制打印第 1 个参数的数值。
 *  例如，to_base_n(129, 8) 显示的结果为 201,也就是129的八进制数。在一个完整的程序中测试该函数。
 */

/*
 *  
 *  
 */

 #include <stdio.h>

 int main(void)
 {

    return 0;
 }
