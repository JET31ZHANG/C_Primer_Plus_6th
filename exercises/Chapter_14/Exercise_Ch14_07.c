/* Exercise_Ch14_07.c --  */

/*
 *  修改程序清单14.14，从文件中读取每条记录并显示出来，允许用户删除记录或修改记录的内容。
 *  如果删除记录，把空出来的空间留给下一个要读入的记录。要修改现有的文件记录，必须用r+b模式，而不是a+b模式。
 *  而且，必须更加注意定位文件指针，防止新加入的记录覆盖现有记录。
 *  最简单的方法是改动存储在内存中的所有数据，然后再把最后的信息写入文件。
 *  其中的一个方法是在book结构中添加一个成员表示是否该项被删除。
 */

/*
 *  
 *  
 */

 #include <stdio.h>

 int main(void)
 {

    return 0;
 }
