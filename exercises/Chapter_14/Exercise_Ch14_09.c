/* Exercise_Ch14_01.c --  */

/*
 *  巨人航空公司（编程练习8）需要另一架飞机（容量相同），每天飞4班（航班102、311、444和519）。
 *  把程序扩展为可以处理4个航班。用一个顶层菜单提供航班选择和退出。
 *  选择一个特定航班，就会出现和编程练习8类似的菜单。但是该菜单要添加一个新选项：确认座位分配。
 *  而且，菜单中的退出时返回顶层菜单。每次显示都要指明当前正在处理的航班号。
 *  另外，座位分配显示要指明确认状态。
 */

/*
 *  
 *  
 */

 #include <stdio.h>

 int main(void)
 {

    return 0;
 }
