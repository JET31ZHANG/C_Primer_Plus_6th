/* Exercise_Ch14_04.c --  */

/*
 *  编写一个程序，创建一个有两个成员的结构模板：
 *  a. 第 1 个成员是社会保险号，第 2 个成员是一个有 3 个成员的结构，第 1 个成员代表名，第 2 个成员代表中间名，第 3 个成员表示姓。
 *     创建并初始化一个内含 5 个该类型的结构数组。该程序以下面的格式打印数据：
 *     Dribble, Flossie M. -- 302039823
 *     如果有中间名，只打印它的第 1 个字母，后面加一个点（.）；
 *     如果没有中间名，则不用打印点。
 *     编写一个程序进行打印，把结构数组传递给这个函数。
 *  b. 修改a部分，传递结构的值而不是结构的地址。
 */

/*
 *  
 *  
 */

 #include <stdio.h>

 int main(void)
 {

    return 0;
 }
