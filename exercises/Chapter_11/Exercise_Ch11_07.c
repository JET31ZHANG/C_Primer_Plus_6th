/* Exercise_Ch11_07.c --  */

/*
 *  strncpy(s1, s2, n) 函数把 s2 中的 n 个字符拷贝到 s1 中，截断 s2，或者有必要的话在末尾添加空字符。 
 *  如果 s2 的长度是 n 或多于 n，目标字符串不能以空字符结尾。该函数返回 s1。
 *  自己编写一个这样的函数，名为 mystrncpy()。在一个完整的程序中测试该函数，使用一个循环给函数提供输入值。
 */

/*
 *  
 *  
 */

 #include <stdio.h>

 int main(void)
 {

    return 0;
 }